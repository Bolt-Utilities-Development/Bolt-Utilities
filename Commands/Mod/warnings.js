module.exports = {
    name: "warnings",
    aliases: ['wg'],
    description: "See the warnings of a user.",
    usage: "Warnings [user]",
    run: async (client, message, args) => {

  message.reply("Please use /Warnings!")}
}